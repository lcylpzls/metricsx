# 安全说明

metricsx 将以下情况视为安全缺陷:

- 非法标签值或指标名导致 panic(拒绝服务);
- 全局注册表被测试或恶意代码污染;
- 指标名冲突静默丢失数据。

## 报告漏洞

- 请勿在公开 issue 中披露漏洞细节;
- 通过邮件 [lcylpzls@qq.com](mailto:lcylpzls@qq.com) 联系维护者,
  并在标题注明 `[Security]`;
- 修复发布前我们会与报告者保持沟通;发布后可应要求公开致谢。

## 安全使用建议

- 测试使用 WithRegistry 独立注册表,避免污染全局;
- 指标名遵循库前缀风格,降低跨库冲突概率;
- 标签值应来自枚举或固定集合,避免高基数标签
  (Prometheus 内存风险由调用方负责);
- 注册失败(名称冲突)静默忽略,部署前用 Gather 核对指标齐全。
